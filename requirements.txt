# Runtime dependencies for PelletLabCineViewer
# Install into a clean virtual environment:  pip install -r requirements.txt
#
# `napari[all]` pulls in the Qt backend, magicgui, and the plugin machinery.
# From napari 0.8 the default backend is PyQt6; if your machine is happier on
# Qt5 see the "Qt backend" note in README.md.

napari[all]>=0.7
pycine>=0.3.2
numpy>=1.24
scipy>=1.10
qtpy>=2.4
scikit-image>=0.22

# Optional: adds a menu of scikit-image filters inside napari
napari-skimage
